# --- Terraform ---------------------------------------------------------
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Variable files that could contain real values (email, secrets, etc.)
# Note: .terraform.lock.hcl is intentionally NOT ignored — it should be
# committed for reproducible provider versions.
terraform.tfvars
*.auto.tfvars

# --- Build artifacts (regenerated by Terraform / CI, not source) -------
lambda/evaluator.zip
function.zip
deployed.zip

# --- Local scratch / test files created during manual testing ----------
kms-test.txt
kms-test-v2.txt
test.txt
test-delete-proof

# --- AWS / credentials (should never be in the repo anyway) ------------
.aws/
*.pem
*.key

# --- OS / editor ---------------------------------------------------------
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp